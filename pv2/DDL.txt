SELECT pg_get_functiondef('public.cleanup_all_expired_vault_roles()'::regprocedure);

SELECT pg_get_functiondef('public.cleanup_expired_vault_roles(text)'::regprocedure);

SELECT pg_get_viewdef('public.vault_roles_summary', true);
